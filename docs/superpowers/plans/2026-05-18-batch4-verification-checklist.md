# Batch 4 — 會計正確性 + 收支流程修正：驗證清單

> Generated: 2026-05-18
> PR: #43 (feat/batch4-accounting-fixes)
> HEAD (after R1): ee5caa1

## How to test

```bash
cd frontend && npm run dev
```

Open browser → localhost:5173. 所有測試步驟均在本地 dev 環境手動操作。

---

## F4-1: 儲值 flash 餘額雙重計算

### 驗證步驟
1. 啟動 App，確認學生列表有帳戶餘額為 0 的學生
2. 搜尋該學生 → 點擊選擇 → 切到「繳費」模式（按 W 鍵或點擊）
我使用q w 切換模式的時候，游標焦點應該要自動在輸入數字的地方
這樣我才可以進行 q -> 500 -> enter ，過程中不需要用到滑鼠。
且焦點就算在輸入數字的地方， q w e 快捷鍵都要能正常運作。

3. 輸入繳費金額 500 → 按 Enter 確認
4. 觀察畫面頂端或中間的 flash banner（確認橫幅）

### 預期行為
- flash banner 顯示 `餘額 $500`（非 $1,000）
- `detail` 顯示 `繳費: 收現 500`
- 右邊 RecentStrip 的最新一筆顯示 `+500`（綠色）
pass

### 邊界條件
- 餘額為 -200 的學生（欠款），儲值 500 → flash 顯示餘額 $300
- 餘額為 500 的學生，繳費 0 → flash 顯示餘額 $500（未變動）
pass

---

## F4-2: 收支完成後狀態卡住

### 驗證步驟
1. 在 idle 首頁點擊「新增 收入/支出」按鈕
2. 輸入金額 150 → 按 Enter
3. 選「支出」→ 選「付便當錢」
4. 觀察 flash banner 出現
5. 點擊 flash banner 關閉按鈕（或等待倒數結束）
6. 再次點擊「新增 收入/支出」按鈕

### 預期行為
- 支出 commit 後出現 flash banner，顯示 `支出: 付便當錢 −$150`
- 關閉 flash 後回到 idle 狀態
- 「新增 收入/支出」按鈕可再次點擊，無需重整網頁
- 重複上述步驟 3 次，每次都能正常完成
pass

### 邊界條件
- 連續輸入 2 筆收入 → 2 次 flash 都能正常 dismiss
- 收入 → 支出 → 收入交替輸入 → 不卡住
- expense flash 點擊「復原」→ transaction 被刪除，不影響學生帳戶

額外發現的問題：
1. 收入/支出 的餘額，必須是櫃台的淨現金
2. 支出->其他原因->輸入備註 或 收入->輸入備註 ，按下enter後，應該也要跳出
櫃台 #
支出: 付便當錢 −$500  餘額 $xxx
−$500
的這個橘色畫面。

---

## F4-3: 收入流程跳過 reason 選擇

### 驗證步驟
1. 點擊「新增 收入/支出」
2. 輸入金額 500 → 按 Enter
3. 在方向選擇畫面，點擊「收入」
4. 觀察接下來出現的畫面
5. 再次操作一次，但選「支出」

### 預期行為
- **選「收入」**：直接進入備註輸入畫面（跳過 reason 選擇），可輸入備註後確認
- **選「支出」**：進入 reason 選擇畫面（付便當錢 / 其他原因）
- 收入 commit 後，RecentStrip 顯示 `+500`（綠色，badge 顯示「收」）
- 支出 commit 後，RecentStrip 顯示 `−150`（紅色，badge 顯示「支」）

### 邊界條件
- 收入備註可留空 → 仍可正常 commit
目前為不可commit，正常行為，必須要有備註才能commit
- 支出「付便當錢」→ 直接 commit，不經過備註輸入
- 支出「其他原因」→ 進入備註輸入

額外發現的問題：
1. RecentStrip中「__cashier__櫃台」的字，英文和中文交接處重疊在一起了。
2. 操作收支的時候，鍵盤的 <- -> 左右鍵無法操作選擇支出或收入、付便當錢或其他原因，因為焦點不在按鈕上面，也無法按下enter，都要用滑鼠操作
3. 輸入收支備註的地方輸入欄位跟外邊框都黏在一起了，鍵盤提示也重在輸入欄位上，非常醜，可以查看圖片 ~/Downloads/f4-3-3.png。

---

## F4-4: 最近 5 筆收入顯示為「收」（綠色）

### 驗證步驟
1. 輸入一筆收入 300（備註：贊助金）
2. 輸入一筆支出 200（備註：買文具）
3. 觀察右側「最近 5 筆」區塊

### 預期行為
- 收入那筆：badge 顯示「收」、背景綠色（type-income）
- 支出那筆：badge 顯示「支」、背景紅色（type-expense）
- 訂餐那筆：badge 顯示「訂」、背景灰色

### 邊界條件
- 只有收入沒有支出的日期 → 所有 expense badge 都是「收」(綠)
- 只有支出沒有收入的日期 → 所有 expense badge 都是「支」(紅)
- 混合收入支出的日期 → 各自正確顯示

「收」的背景不是綠色，字體是咖啡色的樣子，背景好像橘色
小問題，不影響行為，可以微調樣式就好。應該整個 RecentStrip 的部分 css要統一重新分析規劃修改。
然後收支備註，希望可以在 RecentStrip 顯示前4個字 如
支 繳便當費 500
收 贊助金 300

---

## F4-5: 今日帳顯示現金流記錄

### 驗證步驟
1. 輸入幾筆收支（至少 2 筆收入 + 2 筆支出）
2. 切換到「今日帳」tab（F2）
3. 觀察頁面上方 ReportSummaryStats 統計區
4. 向下捲動觀察 LedgerGroupedTable 的 expense section

### 預期行為
- ReportSummaryStats 新增「櫃台收入」「櫃台支出」兩欄：
  - 櫃台收入：顯示收入總額（綠色）+ 筆數
  - 櫃台支出：顯示支出總額（紅色）+ 筆數
總交易好像連訂便當沒繳錢的都算在一起了。
訂便當的數量統計跟 有沒有繳費、收入支出的現金流，應該要是兩個獨立的流程。
訂便當，學生沒繳費，就從儲值金的數值扣除便當錢，學生繳費，就將繳費金額儲值到儲值金且納入櫃台的現金收入。我們櫃台的錢盒裡面實際上是兩種錢「學校的零用金」+「學生的儲值金正負加總的總餘額」

- LedgerGroupedTable 頂端「櫃台 收入/支出」區塊：
  - 收入 row：淡綠背景，type 欄顯示「收入」(綠色粗體)，金額欄 `+$xxx` (綠色)
  - 支出 row：淡紅背景，type 欄顯示「支出」(紅色粗體)，金額欄 `−$xxx` (紅色)
  - 每一筆顯示備註內容和刪除按鈕

### 邊界條件
- 完全沒有收支記錄 → 不顯示「櫃台 收入/支出」區塊
- 只有收入 → type 欄全部顯示「收入」，櫃台支出統計為 0 筆
- 只有支出 → type 欄全部顯示「支出」，櫃台收入統計為 0 筆
- 訂餐學生的交易正常顯示在下方的分組表格中，不與櫃台收支混淆

只看到 櫃台 收入/支出（14 筆）的title，底下完全沒有欄位顯示逐筆交易紀錄。

---

## F4-6: RecentStrip order/payment 用 afterBalance 判斷

### 驗證步驟
1. 找一個餘額為 0 的學生 → 訂餐 90（不繳費）
2. 找一個餘額為 500 的學生 → 訂餐 90（不繳費）
3. 找一個餘額為 50 的學生 → 訂餐 90（不繳費）
4. 找一個餘額為 -400 的學生 → 訂餐 90（不繳費）
5. 找一個餘額為 -90 的學生 → 繳費 50

### 預期行為
| 場景 | afterBalance | 顯示 | 顏色 |
|------|-------------|------|------|
| 餘額 0，訂餐 90 | -90 | 待繳費 90 | 紅 |
| 餘額 500，訂餐 90 | 410 | 已繳費 90 | 綠 |
| 餘額 50，訂餐 90 | -40 | 待繳費 40 | 紅 |
| 餘額 -400，訂餐 90 | -490 | 待繳費 490 | 紅 |
| 餘額 -90，繳費 50 | -40 | 待繳費 40 | 紅 |

### 邊界條件
- 全額繳清 → `afterBalance = 0` → `>= 0` → 顯示「已繳費」(綠)
- 繳費 500 後 afterBalance >= 0 → 顯示 `+$500` (綠)
- order 的 paidAmount 不直接用於判斷，完全以 afterBalance 為準

實際測試同一個同學訂多個便當，再超額繳費的狀況如下

07:17:38 006 黃詩涵 繳 +500
07:10:58 006 黃詩涵 訂 待繳費 360
07:10:56 006 黃詩涵 訂 待繳費 270
07:10:53 006 黃詩涵 訂 待繳費 180
07:09:23 006 黃詩涵 訂 待繳費 90

問題點：
1. 訂4個便當的這樣顯示方式，到底是要繳 90+180+270+360？還是 360?
是否重複訂便當的合併成為一筆資料就好，寫 「訂x4 待繳費 360」然後點擊該筆記錄可以展開來看訂購紀錄？
因為有可能是訂 90x2 + 60x2 待繳費 300
2. 如果繳費500元，超過了360待繳費，應該把待繳費的變成已繳費 360，儲值140，才會清楚吧
如果是重複訂便當的銷帳方式就是按優先順序銷帳，留下最後一筆沒繳清的 寫 待繳費 30

---

## F4-7: 開帳金額可重複儲存

### 驗證步驟
1. 切換到「設定」tab（F3）
2. 在「每日開帳金額」輸入 4000 → 點擊「儲存開帳金額」
3. 觀察按鈕旁出現「已儲存」提示（2 秒後消失）
4. 再次修改為 5000 → 點擊「儲存開帳金額」
5. 觀察是否成功儲存

### 預期行為
- 第一次儲存：呼叫 openCashSession 建立 session → 成功
- 第二次儲存：呼叫 updateOpeningCash 更新 → 成功，顯示「已儲存」
- 第三次儲存：同上，可無限次更新

### 邊界條件
- 日期已關帳（dateStatus === 'closed'）→ 開帳金額 input 和儲存按鈕 disabled
- 輸入負數 → 不儲存（前端檢查 >= 0）
- 輸入非數字 → 不儲存（Number.isFinite 檢查）
- 關帳後重開（reopen）→ 可再次修改開帳金額

因為按下關帳後出現「報表區塊發生錯誤」所以無法驗證跨兩日的開關帳行為，但目前開帳金額是可以改沒錯。
我們期待的是今天關帳金額，就會等於明天的開帳金額。修改今天的開帳金額，不影響昨天的關帳金額。每天的報表是獨立的，沒有依存關係、沒有計算依存關係

---

## 整合測試（全部 F4-1 ~ F4-7）

### 完整流程
1. 啟動 App → Admin 設定開帳金額 4000
2. 修改開帳金額為 5000 → 確認可儲存 → 「已儲存」
3. 回到 POS → 搜尋學生「王小美」（餘額 0）→ 訂餐 90 → 確認 flash 顯示「餘額 −$90」
4. 對王小美儲值 200 → flash 顯示「餘額 $110」（非 $310）
5. 新增收入 300（贊助金）→ 直接進備註 → 輸入「贊助金」→ commit
6. 新增支出 150（買文具）→ 選「支出」→ 選「其他原因」→ 輸入「買文具」→ commit
7. 觀察 RecentStrip：
   - 王小美儲值：badge「繳」，金額 `+$200`，afterBalance 110 → 綠
   - 收入：badge「收」(綠)，金額 `+$300`
   - 支出：badge「支」(紅)，金額 `−$150`
8. 確認「新增 收入/支出」按鈕仍可使用
9. 切到「今日帳」→ 確認：
   - 櫃台收入：$300 (1 筆)
   - 櫃台支出：$150 (1 筆)
   - expense section 分列收入/支出，各有備註
10. 回 POS → 王小美的 order 顯示「已繳費 90」（因 afterBalance = 110 >= 0）
11. 選「葉大雄」（餘額 0）→ 訂餐 90 → 顯示「待繳費 90」(紅)
12. 再對葉大雄繳費 50 → 顯示「待繳費 40」(紅)（因 afterBalance = -40）
13. 全部測試完成，無需重整網頁

額外發現：
1. 如果學生今天已經訂過便當，今天在次輸入學生編號進入訂便當介面的時候，希望預設是在 w 繳費 的選項
2. 新增 收入/支出 希望新增快捷鍵 A

---

## Test suite status

- t1: `npx tsc --noEmit` — PASS
- t2: `npm run lint` — PASS
- t3: `npx vitest run` — 363 tests pass
- t4: `npm run build` — PASS
