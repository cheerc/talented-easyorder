# Batch 5-6 Fixes Retest Checklist

> 驗證 PR #50 (e445515) 是否修復了原始驗證中發現的所有問題
> Date: 2026-05-21
> 基於: `docs/superpowers/plans/2026-05-21-batch5-6-verification-checklist.md` 中使用者標注的問題

## How to test

```bash
cd ~/talented-easyorder/frontend && npm run dev
```

Open browser → localhost:5173

---

## §1.1: 搜尋框聚焦行為（3 個修復點）

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 啟動 App → idle 首頁載入完成 | 搜尋框**不聚焦**（游標不在搜尋框內） | 原本剛載入就聚焦 | |
| 2 | 無焦點時按 `1` | 搜尋框自動聚焦 + `1` 已填入 | 原本按數字不會聚焦 | |
| 3 | 接著按 `1` → 搜尋框顯示 `11` | 可連續輸入多位數（11, 012, 22） | 原本只能輸入單個數字 | |
| 4 | 搜尋框有內容時按 `Escape` | 清除數字 + blur 離開焦點 | 原本 Escape 無此行為（你要求新增） | |
| 5 | 搜尋框空白時按 `Enter` | blur 離開焦點 | pass（維持不變） | |

---

## §1.2: 「新增 收入/支出 (A)」按鈕

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | idle 首頁觀察按鈕文字 | 顯示「新增 收入/支出 **(A)**」（只有一個 A） | 原本顯示 (A) (A) 兩個 | |
| 2 | 按鈕樣式與 Q/W/E 統一 | `(A)` 使用相同的 `.mode-key` 樣式 | 原本風格不統一 | |

---

## §1.4: 收支流程 Escape 返回上一層

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | A → 輸入金額 → Enter → 類型選擇 → 按 `Escape` | 返回金額輸入（上一層），不是直接回 idle | 原本 Escape 直接回 idle | |
| 2 | 類型選擇 → 選「支出」→ 原因選擇 → 按 `Escape` | 返回類型選擇（上一層） | 同上 | |
| 3 | 收入路徑 → 備註輸入 → 按 `Escape` | 返回類型選擇（收入無原因步驟） | 同上 | |
| 4 | 支出路徑 → 原因 → 備註輸入 → 按 `Escape` | 返回原因選擇 | 同上 | |

---

## §2.1: 數值輸入框 DRY + 滾輪 + 快捷鍵 E

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 繳費金額框聚焦時按 `e` | `e` 被攔截（不輸入），但全域快捷鍵 E（取消訂餐）仍可觸發 | 原本 e 被擋後 E 快捷鍵也失效 | |
| 2 | 任意數值輸入框聚焦 → 滑鼠滾輪上下滾 | 數值**不變**（滾輪行為被禁止） | 原本滾輪會增減數值 | |
| 3 | 檢查 6 個數值輸入框是否使用同一個 `NumericInput` 組件 | 統一使用共用組件（DRY） | 原本各自重複實作 | |

### 6 個輸入框逐一驗證

| 輸入框位置 | 輸入 `-` `.` `e` | 滾輪 | Pass? |
|-----------|-----------------|------|-------|
| 收支金額（A → 金額） | 全部被擋 | 不變 | |
| 繳費金額（學生卡片 payment） | 全部被擋 | 不變 | |
| 改價金額（學生卡片 priceOverride） | 全部被擋 | 不變 | |
| 便當單價（設定頁） | 全部被擋 | 不變 | |
| 開帳金額（設定頁） | 全部被擋 | 不變 | |
| 點算現金（關帳 countedCash） | 全部被擋 | 不變 | |

---

## §3.1: 價格修改區域排版 + 便當統計

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 搜尋學生 → 點「訂購其他餐點」展開自訂價格 | 有虛線分隔、卡片容器、間距舒適 | 原本全部擠在一起 | |
| 2 | 訂購一筆非預設便當（改價/改品項）→ 回 idle 首頁看統計 | 非預設便當**不計入**今日便當統計 | 原本統一計入導致數量錯誤 | |

---

## §3.2: RecentStrip 筆數

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 右側側邊欄 RecentStrip 標題 | 顯示「最近 **12** 筆」 | 原本只有 5 筆 | |
| 2 | 有足夠交易時列表顯示筆數 | 最多顯示 12 筆 | 同上 | |

---

## §3.4: RecentStrip 收支重複標籤

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 新增一筆支出（備註「遺失費用」）→ 觀察 RecentStrip | `支 遺失費用 −300`（左側徽章 + 備註 + 金額） | 原本顯示「支 支 遺失費用 −300」重複 | |
| 2 | 新增一筆收入（備註「撿到錢」）→ 觀察 RecentStrip | `收 撿到錢　　 +3`（左側徽章 + 備註對齊 + 金額） | 原本顯示「收 收 撿到錢 +3」重複 | |
| 3 | 備註超過 4 字的情況 | 備註截斷為 4 字 + `…`，金額不被壓縮 | 新增的 overflow 處理 | |

---

## §4.1: 關帳按鈕 + 關帳崩潰修復

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 進關帳 → 不填備註 → 觀察按鈕 | disabled、半透明、cursor: not-allowed | pass（維持） | |
| 2 | 填入備註 → 按「確認關帳」 | 正常關帳，**不崩潰** | 原本出現「報表區塊發生錯誤」 | |
| 3 | 關帳後重新整理 | 狀態正確保持為已關帳 | 原本重新整理後變回未關帳 | |

---

## §4.2: 帳本表格展開 + 分頁

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 今日帳 → 帳本 LedgerGroupedTable | 表格完整展開，欄位對齊（7 欄） | 原本表格不完整、沒展開 | |
| 2 | 表頭與細節列對齊 | padding 一致，樹狀符號 `└` 位置正確 | 原本欄位偏移 | |
| 3 | 超過 20 筆 → 分頁控制 | 上一頁/下一頁 + 頁碼顯示 | 原本沒有分頁 | |
| 4 | 櫃台收支列（非學生） | 無 `└` 樹狀符號 | 新增的 `.counter-row` 處理 | |

---

## §4.3: 首次開帳金額 ConfirmDialog

| # | 測試步驟 | 預期結果 | 原始問題 | Pass? |
|---|---------|---------|---------|-------|
| 1 | 新日期（無 cashSession）→ 設定開帳金額 → 儲存 | 跳出 ConfirmDialog：「確定要將今日開帳金額設為 $X 嗎？」 | 原本首次設定不跳 dialog（你要求要跳） | |
| 2 | Dialog 標題 | 「設定開帳金額」（非「修改」） | 新增：首次 vs 修改不同文案 | |
| 3 | Dialog 按鈕 | 「確認設定」（primary 藍色） | 新增：首次用 primary，修改用 danger | |
| 4 | 已有 cashSession → 修改金額 → 儲存 | Dialog 標題「修改開帳金額」、按鈕「確認修改」（danger 紅色） | 區分首次 vs 修改 | |

---

## Summary

| Section | 修復點數 | 測試項目數 |
|---------|---------|----------|
| §1.1 搜尋框聚焦 | 3 | 5 |
| §1.2 按鈕 DRY | 2 | 2 |
| §1.4 Escape 上一層 | 1 | 4 |
| §2.1 數值輸入 DRY | 3 | 9 |
| §3.1 排版+統計 | 2 | 2 |
| §3.2 RecentStrip 筆數 | 1 | 2 |
| §3.4 重複標籤 | 1 | 3 |
| §4.1 關帳崩潰 | 1 | 3 |
| §4.2 帳本表格 | 1 | 4 |
| §4.3 首次開帳 dialog | 1 | 4 |
| **合計** | **16** | **38** |
