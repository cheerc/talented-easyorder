// Re-export useFirebase for consumer convenience.
// Implementation lives in providers/FirebaseProvider.tsx (co-located with the context).
export { useFirebase } from '../providers/FirebaseProvider';
